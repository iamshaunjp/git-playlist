print("I felt like you needed some R code. However I realise this is pretty indistinugishable from Python right now so let's write a loop.")

for(num in 1:10) {
    print("Run this, Python!")
    if(num == 10) {
        print("See? That's how it's done")
    }
}
